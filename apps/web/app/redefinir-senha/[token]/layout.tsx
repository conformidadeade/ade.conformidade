import type { Metadata } from "next";

export const metadata: Metadata = {
  title: { absolute: "Redefinir senha — ADE" },
};

export default function RedefinirSenhaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
